server:
 port:
  8082
zuul:
  routes:
    first-client:
      path: /**
      serviceId: first-client

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    routes:
      enabled: true
