#Used for all profiles

#eureka section
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server.port: 8761

#spring section
spring:
  cloud:
    config:
      fail-fast: true

#'docker' profile only
---

#spring section
spring:
  profiles:
    active: docker
  application:
    name: marakogr-eureka
  cloud:
    config:
      discovery:
        enabled: false
      uri: http://${CONFIG_SERVER_HOST}:${CONFIG_SERVER_PORT}
      fail-fast: true
