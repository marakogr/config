#Used for all profiles

#spring section
spring:

#spring cloud
  cloud:
    config:
      fail-fast: true

#actuator
management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /actuator

test: hello

server.port: 8085


#'docker' profile only
---
#spring section
spring:
  profiles:
    active: docker
  application:
    name: marakogr-client

#spring cloud
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST}:${CONFIG_SERVER_PORT}
      fail-fast: true
      discovery:
        enabled: false
